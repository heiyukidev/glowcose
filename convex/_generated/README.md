This directory is generated by `npx convex dev` (or `npx convex codegen` once a deployment is linked).

Committed so Next.js and Expo can import `api` without a live Convex project. After `npx convex dev`, these files are regenerated — keep them in git.
